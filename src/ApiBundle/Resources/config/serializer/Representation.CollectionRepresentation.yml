ApiBundle\Representation\CollectionRepresentation:
    relations:
        -
            rel: self
            href:
                route: expr(object.getRoute())
                parameters: expr(object.getParameters())
    exclusion_policy: ALL
    virtual_properties:
        getResources:
            serialized_name: data
            type: array
